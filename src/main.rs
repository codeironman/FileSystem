mod file;

use fuser::MountOption;

fn main() {
    let mountpoint = "";
    println!("yes");
    //let fs = File::new();
    //fuser::mount2(fs, mountpoint, &[MountOption::FSName("myfs".to_string())]).unwrap();
}
